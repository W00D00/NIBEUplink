# -*- coding: utf-8 -*-

# https://api.nibeuplink.com/docs/v1/Authentication
# It is up to the client owner to make sure these are not leaked to a third party! Keep it secret!
NIBE_UPLINK_AUTHENTICATION = {
    'username': "",
    'password': "",
    'clientId': '',
    'clientSecret': '=',
    'systemIDs': {'': '',}
    }
